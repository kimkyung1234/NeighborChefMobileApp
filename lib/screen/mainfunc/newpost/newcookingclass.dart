import 'package:flutter/material.dart';

class NewCookingClass extends StatelessWidget {
  const NewCookingClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
