import 'package:flutter/material.dart';

class NewRecipeShared extends StatelessWidget {
  const NewRecipeShared({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
