import 'package:flutter/material.dart';

class NewFoodMarket extends StatelessWidget {
  const NewFoodMarket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
